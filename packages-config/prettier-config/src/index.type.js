/**
 * @typedef {import("prettier").Config} PrettierConfig
 *
 * @typedef {object} Data              - a data for generate prettier configs
 * @property {PrettierConfig} [custom] - custom config
 */

module.exports = {};
